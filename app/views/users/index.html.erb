
  <h1>All Users</h1>
  <% @users.each do |user| %>
    <li><%= link_to user.name, user_path(user) %> |
      <%= link_to "Delete", user_path(user),
      :data => {:confirm => "Are you sure?",
        :method => 'delete'}%>
    </li>
  <% end %>
